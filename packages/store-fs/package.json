{
  "name": "@iris/store-fs",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "description": "Iris serverless-style host adapter — a StateStore + Scheduler over node:fs (cold per-turn, no held process) enforcing the SAME CAS/fencing/hwm/snapshot invariants as the sqlite store. Host B for the cross-host portability proof. Not a determinism bypass.",
  "exports": "./src/index.ts",
  "dependencies": {
    "@iris/core": "*"
  }
}
