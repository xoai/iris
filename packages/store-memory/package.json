{
  "name": "@iris/store-memory",
  "version": "0.0.0",
  "type": "module",
  "description": "Iris in-memory host adapter — a real StateStore + Scheduler that enforces CAS/fencing/hwm (store B for cross-store resume; fast unit store). Not a determinism bypass.",
  "exports": "./src/index.ts",
  "dependencies": {
    "@iris/core": "*"
  }
}
