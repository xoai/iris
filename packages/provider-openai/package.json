{
  "name": "@irisrun/provider-openai",
  "version": "0.1.0",
  "type": "module",
  "description": "Iris model_call performer — direct OpenAI Chat Completions adapter via built-in fetch (zero deps). Real adapter runs only with OPENAI_API_KEY; tests use a fake fetch / fake model. A peer of @irisrun/provider-anthropic behind the same model port.",
  "exports": {
    ".": {
      "iris-src": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "dependencies": {
    "@irisrun/core": "^0.1.0"
  },
  "license": "MIT",
  "engines": {
    "node": ">=24"
  },
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/xoai/iris.git",
    "directory": "packages/provider-openai"
  },
  "homepage": "https://github.com/xoai/iris#readme",
  "files": [
    "dist"
  ]
}
